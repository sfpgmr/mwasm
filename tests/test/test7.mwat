
{@
 // JS コードブロック
 $.circleArea = r =>{
   return r * r * Math.PI;
 }
 
 $.array = [0,1,2,3,4,5];

}

(module
  (memory $memory 1 )
  (export "memory" (memory $memory))
  (export "test" (func $test))
  (func $test (result f32)
    (f32.const %circleArea(1) + array[4];)
  )
)
