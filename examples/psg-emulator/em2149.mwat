(@memorymap offset 0
  ;;; comment
  i32 voltable[16*2+$.X[3]] {0};;;comment
  {
    $$.defines = 

  }
  i32 data,data1,data2;
)

(module
  (memory $memory 1 
    (data $voltable ($const offset ))
  )
  (export "memory" (memory $memory))
  (export "test" (func $test))
  (func $test (result i32)
    i32.const %voltable
    i32.load
    i32.const %voltable data
  )
)
